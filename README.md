# Setup:
1) Ensure you have Python 3 on your machine (visit https://www.python.org/downloads/).
2) Ensure that python3-venv is installed (`sudo apt install python3-venv` if on WSL).
3) run `bash setup.sh`
4) Install recommended extensions (Appears under the `Extensions` tab on the left).
5) You're done!